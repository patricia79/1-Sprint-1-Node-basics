class Calculadora {
    calcularSuma(a, b) {
        
        return a + b;
    }

    calcularResta(a, b) {
        return a - b;
    }

    calcularMultiplicacio(a, b) {
        return a * b;
    }

    calcularDivisio(a, b) {
        return a / b;
    }
}

module.exports = Calculadora;